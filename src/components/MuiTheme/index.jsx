import { createTheme, colors } from "@mui/material";

export const theme = createTheme({
  status: {
    danger: colors.red[100],
  },
  palette: {
    secondary: { main: colors.orange[500] },
    qizgish: { main: colors.red[100] },
  },
});
